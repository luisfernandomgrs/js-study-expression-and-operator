// Operadores de String (String Operators)

// Comparison (comparação)
console.log('a' == 'a');

// concatenation (concatenação)
let alpha = "alpha";

console.log("a" + "b");
console.log(alpha + "bet");
console.log(alpha + "bet" + "s");
console.log(alpha += "bet");
console.log(alpha);

alpha += ": ";
console.log(alpha + 250);